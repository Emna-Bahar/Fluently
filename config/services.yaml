# This file is the entry point to configure your own services.
# Files in the packages/ subdirectory configure your dependencies.

parameters:
    groq_api_key: '%env(GROQ_API_KEY)%'

services:
    # default configuration for services in *this* file
    _defaults:
        autowire: true      # Automatically injects dependencies in your services.
        autoconfigure: true # Automatically registers your services as commands, event subscribers, etc.
        bind:
            string $aiApiKey: '%env(string:AI_API_KEY)%'
            string $aiBaseUrl: '%env(string:AI_BASE_URL)%'
            string $aiModel: '%env(string:AI_MODEL)%'

    # makes classes in src/ available to be used as services
    App\:
        resource: '../src/'
        exclude:
            - '../src/DependencyInjection/'
            - '../src/Entity/'
            - '../src/Kernel.php'

    # Configuration pour HttpClient
    Symfony\Contracts\HttpClient\HttpClientInterface:
        alias: 'http_client'

    # Ton service DictionaryService
    App\Service\DictionaryService:
        arguments:
            $httpClient: '@Symfony\Contracts\HttpClient\HttpClientInterface'

    #App\Service\AITextCorrectionService:
        #arguments:
            #$geminiApiKey: '%gemini_api_key%'
    App\Service\AITextCorrectionService:
        arguments:
            $groqApiKey: '%groq_api_key%'